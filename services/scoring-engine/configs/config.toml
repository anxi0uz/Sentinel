workers = 0 # 0 = runtime.NumCPU()*2
cacheInterval = "5m"

[kafka]
brokers = ["localhost:9092"]

[database]
host = "localhost"
port = 5432
user = "postgres"
password = ""
name = "sentinel"
sslmode = "disable"
